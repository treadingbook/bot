# 📟 Cyber Video Downloader Bot
A high-speed Telegram bot to download videos using `yt-dlp`.

### 🚀 Features
- Fast video extraction
- Auto-clean (deletes files after sending)
- Hacker-style terminal responses

### 🛠 Setup
1. Install requirements: `pip install -r requirements.txt`
2. Set your `YOUR_BOT_TOKEN` in environment variables.

3. Run: `python bot.py`
